user name , user id , user title get with axios request
every user have comments, when clicked 'view comment' this user
if user hasn't comments return <div> 'No comments' </div>
have back button when you news/{id}
